services:
  - type: web
    name: anythingllm-web
    env: docker
    repo: https://github.com/YOUR_USERNAME/anything-llm
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true

  - type: worker
    name: anythingllm-collector
    env: docker
    repo: https://github.com/YOUR_USERNAME/anything-llm
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    command: npm run collector
